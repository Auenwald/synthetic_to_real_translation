# (1) Download all datasets (see README-files in synthia, synthiastyle and cityscapes)

# (2) Create a virtual environment

python3 -m venv venv

# (3) Activate the virtual enviroment (they are different commands depending on the used shell)
$ . venv/bin/activate
$ source venv/bin/activate

# (4) Install all packages
pip install -r requirements.txt

# (5) Run train.sh